## No hover
Shows the links and checkboxes which Adminer reveals only when hovering their row: the edit links in select, Alter on the structure page, select in the menu, Edit next to a query and the row checkboxes.

Colors are not changed, both the light and the dark mode stay in use.

This was the default behavior before Adminer 5.6.0.
