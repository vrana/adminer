## Border
Puts the cells with the repeated links and row checkboxes back in the table: they get the same border and background as the other cells instead of looking like they lie outside the table.

Colors are not changed, both the light and the dark mode stay in use.

This was the default appearance before Adminer 6.0.0.
